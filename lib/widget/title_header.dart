import 'package:flutter/material.dart';

import '../model/play_list.dart';
class TitleHeader extends StatelessWidget {
  const TitleHeader({Key? key,required this.play}) : super(key: key);

final PlayList play;

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [

        ],
      ),
    );
  }
}
